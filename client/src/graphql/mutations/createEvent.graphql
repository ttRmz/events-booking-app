mutation createEvent($title: String!, $description: String!, $price: Float!, $date: Date!) {
  createEvent(EventInput: { title: $title, description: $description, price: $price, date: $date }) {
    title
  }
}
